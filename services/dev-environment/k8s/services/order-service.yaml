apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: ed-namespace
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        image: your-registry/order-service:1.0
        ports:
        - containerPort: 80
        env:
        - name: ConnectionStrings__PostgreSQL
          value: "Host=ed-postgres;Port=5432;Database=order_db;Username=postgres;Password=$(POSTGRES_PASSWORD)"
        - name: RabbitMQ__Host
          value: "ed-rabbitmq"
        - name: RabbitMQ__UserName
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: rabbitmq-username
        - name: RabbitMQ__Password
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: rabbitmq-password
        - name: Seq__Host
          value: "http://ed-seq"
        envFrom:
        - configMapRef:
            name: app-config
        - secretRef:
            name: app-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: ed-namespace
spec:
  selector:
    app: order-service
  ports:
  - port: 80
    targetPort: 80